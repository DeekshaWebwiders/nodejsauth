/**
 * userModel.js
 * -----------------
 * Defines metadata for the 'users' table used by the repository.
*/

module.exports = {
  tableName: 'users', // Should match the actual table name in your database
};
